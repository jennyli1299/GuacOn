<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guacon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Profile.PublicProfile"></activity>
        <activity android:name=".Profile.RecipeForm" />
        <activity android:name=".SearchResult" />
        <activity android:name=".Profile.CardResult" />
        <activity android:name=".Profile.Profile" />
        <activity android:name=".Recipe_Detail" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Login.SignupActivity" />
        <activity android:name=".Login.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>